# Codex Local Supplement

This project uses the root `AGENTS.md` as the main instruction file.

Additional Codex-specific guidance:

- Prefer reading `AGENTS.md` and `CLAUDE.md` together when a task starts.
- Treat the root `AGENTS.md` as the canonical project rule set.
- Prefer project-local conventions over generic Codex defaults when they conflict.
- For multi-service changes, summarize affected services before editing.
- For Docker-related work, assume the user validates through `docker compose up --build`.
